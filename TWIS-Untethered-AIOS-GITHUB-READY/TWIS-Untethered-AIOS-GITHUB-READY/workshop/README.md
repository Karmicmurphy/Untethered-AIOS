# Workshop Baseline Placeholder

Do not manually rebuild the Workshop in this directory.

This directory is intentionally empty in the bootstrap package.

Codex must first authenticate:

`C:\TWIS_FLASHRIVER_REVIEW_READY\TWIS`

Then copy a **code-safe verified snapshot** here.

Do not copy:

- SQLite/runtime databases;
- `.env` or secrets;
- credentials/tokens;
- cookies/browser profiles;
- private recovered archives;
- user project artifacts;
- large generated media;
- model weights;
- transient caches;
- evidence that contains secrets/private content.

After import, record the copied baseline hash under `evidence/`.
