# Harmless Worker Fixture

Twis Holo can count these words without using private source material.

## Deterministic Section

The reference worker reads this local fixture and writes bounded JSON metadata.
