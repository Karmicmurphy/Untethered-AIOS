# Codex Prompt — FlashRiver Follow-up

Follow `AGENTS.md` and `docs/CODEX_DEFAULT_PRESET.md`.

Run the FlashRiver intake locally, verify the imported manifest and artifacts, then improve the review workflow without changing the boundary: raw source archives stay local; GitHub gets safe code/docs/tests only.
