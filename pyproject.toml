[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "twis-untethered-aios"
version = "0.2.0"
description = "Experimental operating layer for Twis Holo Workshop"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
