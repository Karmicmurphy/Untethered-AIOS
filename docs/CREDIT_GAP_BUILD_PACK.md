# CREDIT-GAP BUILD PACK V0.1

## Authority / integration status

The latest state visible on the official GitHub remote when this pack was built is Campaign 2 at:

`bab64c263e337400e0a7b77bcebb47677cd701da`

The user-provided Campaign 3 verification report records a newer local-only verified head:

`5c7c9146bc552397b4a5b78462e54e0c04f2adb8`

Campaign 3 was explicitly reported as not pushed. This prep branch therefore does **not** recreate, overwrite, or replace Campaign 3. It is additive code prepared on a separate branch for later integration review.

## Built now

- deterministic native RULE registry and one read-only policy rule;
- Tri-Memory candidate: exact SQLite truth, deterministic sparse associative recall, temporal transition prediction;
- River Blackboard shared work state with NEED/CLAIM/RESULT/BLOCKED/FAILED/PROVEN/EXPIRED and event traces;
- Capability Cell prep contract;
- declarative MicroForge core and blueprint overlay-plan recipe;
- Cognitive Downshift repeated-trace detector and protected-event promotion denial;
- Capability Metabolism advisory engine;
- Conversational Continuity store preserving OWNER CONTEXT != AI IDENTITY;
- replaceable Model Gateway with explicit paid/network gates;
- endogenous capability-evolution state machine with legal/proof gates and no silent authority expansion.

## Standalone proof

A matching downloadable build pack was run in an isolated Python environment using only stdlib + SQLite. It passed 29/29 prep unit tests and a 10/10 synthetic prep benchmark. These results are **prep-only**, not a replacement for the local Untethered regression/rollback/Workshop proof.

## Intended integration order when Codex returns

1. Authenticate the exact local Campaign 3 head and preserve/push it if still clean.
2. Integrate `prep/rule_lane.py` into the verified budgeted cheap-execution bridge as Campaign 4, adapting the hook to the exact Campaign 3 handler/checkpoint signature.
3. Run Campaign 4 full route/capability/budget/recovery/reuse/conflict verification.
4. Integrate `tri_memory.py` and `river_blackboard.py` as the next cognitive campaign rather than rewriting them from scratch.
5. Integrate `capability_cell.py` only where it complements—not duplicates—Campaign 3 budget/recovery machinery.
6. Use `microforge.py` as the declarative recipe core; bind real image/audio/CAD primitives only after source/license/resource proof.
7. Feed real receipts/traces to `downshift.py`; upgrade the small detector only if measurements justify a stronger salvaged mechanism.
8. Keep metabolism, continuity, model gateway, and evolution behind their later proof gates.

## Non-claims

This branch is **PREP**, not VERIFIED CANDIDATE, LIVE-VALIDATED, or DEPLOYED. It does not touch the authoritative Workshop, does not contain a real model, does not provide hostile-code isolation, and does not auto-promote learned capabilities into protected authority.
